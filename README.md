# codebase-readme-generator

Generates a structured `README.md` by analyzing a target codebase.

## Getting Started

Use the walkthrough in `references/usage-guide.md`.

## Full CLI Docs

See `references/api.md` for all flags, arguments, and exit codes.

## Prerequisites

- `bash`
- Standard Unix tools (`find`, `awk`, `sed`)
- `jq` (optional, improves `package.json` script detection)
